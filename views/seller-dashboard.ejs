<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Flipkart Seller Dashboard - <%= sellerData.profile.businessName %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .dashboard-header {
            background: linear-gradient(135deg, #2874f0 0%, #1c5aa8 100%);
            color: white;
            padding: 2rem 0;
        }
        .flipkart-logo {
            font-size: 1.8rem;
            font-weight: bold;
            color: white;
            text-decoration: none;
        }
        .flipkart-logo:hover {
            color: #ffd700;
        }
        .seller-badge {
            background: #ffd700;
            color: #2874f0;
            padding: 0.25rem 0.5rem;
            border-radius: 15px;
            font-size: 0.8rem;
            font-weight: bold;
            margin-left: 10px;
        }
        .card {
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            border: none;
            margin-bottom: 1.5rem;
        }
        .stat-card {
            background: linear-gradient(135deg, #2874f0 0%, #5a8dee 100%);
            color: white;
        }
        .stat-card .card-body {
            padding: 1.5rem;
        }
        .product-status.active {
            background: #28a745;
            color: white;
            padding: 0.25rem 0.5rem;
            border-radius: 0.25rem;
            font-size: 0.8rem;
        }
        .order-status {
            padding: 0.25rem 0.5rem;
            border-radius: 0.25rem;
            font-size: 0.8rem;
            text-transform: capitalize;
        }
        .order-status.delivered { background: #d4edda; color: #155724; }
        .order-status.shipped { background: #cce5ff; color: #004085; }
        .order-status.processing { background: #fff3cd; color: #856404; }
        
        /* Chatbot Styles */
        .chatbot-container {
            position: fixed;
            bottom: 20px;
            right: 20px;
            width: 350px;
            height: 500px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            display: none;
            flex-direction: column;
            z-index: 1000;
        }
                 .chatbot-header {
             background: linear-gradient(135deg, #2874f0 0%, #1c5aa8 100%);
             color: white;
             padding: 15px;
             border-radius: 15px 15px 0 0;
             display: flex;
             justify-content: space-between;
             align-items: center;
         }
        .chatbot-messages {
            flex: 1;
            padding: 15px;
            overflow-y: auto;
            max-height: 350px;
        }
        .message {
            margin-bottom: 10px;
            padding: 10px;
            border-radius: 10px;
        }
        .message.user {
            background: #e3f2fd;
            margin-left: 20px;
            text-align: right;
        }
        .message.bot {
            background: #f5f5f5;
            margin-right: 20px;
        }
        .chatbot-input {
            padding: 15px;
            border-top: 1px solid #eee;
            border-radius: 0 0 15px 15px;
        }
                 .chatbot-toggle {
             position: fixed;
             bottom: 20px;
             right: 20px;
             width: 60px;
             height: 60px;
             background: linear-gradient(135deg, #2874f0 0%, #1c5aa8 100%);
             color: white;
             border: none;
             border-radius: 50%;
             font-size: 24px;
             cursor: pointer;
             box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
             z-index: 999;
         }
        .loading {
            display: none;
            text-align: center;
            color: #666;
            font-style: italic;
        }
    </style>
</head>
<body>
    <!-- Dashboard Header -->
    <div class="dashboard-header">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-2">
                    <a href="#" class="flipkart-logo">
                        <i class="fas fa-shopping-cart me-2"></i>Flipkart
                    </a>
                    <span class="seller-badge">SELLER</span>
                </div>
                <div class="col-md-6">
                    <h2><i class="fas fa-store me-2"></i><%= sellerData.profile.businessName %></h2>
                    <p class="mb-0">Welcome back, <%= sellerData.profile.ownerName %>!</p>
                </div>
                <div class="col-md-4 text-end">
                    <div class="d-flex flex-column align-items-end">
                        <div class="d-flex align-items-center mb-1">
                            <i class="fas fa-star text-warning me-1"></i>
                            <span class="h5 mb-0"><%= sellerData.profile.rating %></span>
                        </div>
                        <small class="text-light opacity-75">Seller Rating</small>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="container mt-4">
        <!-- Analytics Cards -->
        <div class="row mb-4">
            <div class="col-md-3">
                <div class="card stat-card">
                    <div class="card-body text-center">
                        <i class="fas fa-rupee-sign fa-2x mb-2"></i>
                        <h3>₹<%= sellerData.analytics.totalRevenue.toLocaleString('en-IN') %></h3>
                        <p>Total Revenue</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card stat-card">
                    <div class="card-body text-center">
                        <i class="fas fa-shopping-cart fa-2x mb-2"></i>
                        <h3><%= sellerData.analytics.totalOrders %></h3>
                        <p>Total Orders</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card stat-card">
                    <div class="card-body text-center">
                        <i class="fas fa-clock fa-2x mb-2"></i>
                        <h3><%= sellerData.analytics.pendingOrders %></h3>
                        <p>Pending Orders</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card stat-card">
                    <div class="card-body text-center">
                        <i class="fas fa-chart-line fa-2x mb-2"></i>
                        <h3><%= sellerData.analytics.monthlyGrowth %>%</h3>
                        <p>Monthly Growth</p>
                    </div>
                </div>
            </div>
        </div>

        <div class="row">
            <!-- Business Profile -->
            <div class="col-md-4">
                <div class="card">
                    <div class="card-header">
                        <h5><i class="fas fa-user-tie me-2"></i>Business Profile</h5>
                    </div>
                    <div class="card-body">
                        <p><strong>Owner:</strong> <%= sellerData.profile.ownerName %></p>
                        <p><strong>Email:</strong> <%= sellerData.profile.email %></p>
                        <p><strong>Phone:</strong> <%= sellerData.profile.phone %></p>
                        <p><strong>GST Number:</strong> <%= sellerData.profile.gstNumber %></p>
                        <p><strong>Address:</strong> <%= sellerData.profile.address %></p>
                        <p><strong>Total Sales:</strong> ₹<%= sellerData.profile.totalSales.toLocaleString('en-IN') %></p>
                    </div>
                </div>
            </div>

            <!-- Products -->
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h5><i class="fas fa-box me-2"></i>Products (<%= sellerData.products.length %>)</h5>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>Product</th>
                                        <th>Price</th>
                                        <th>Stock</th>
                                        <th>Category</th>
                                        <th>Status</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% sellerData.products.forEach(product => { %>
                                    <tr>
                                        <td>
                                            <strong><%= product.name %></strong><br>
                                            <small class="text-muted"><%= product.description %></small>
                                        </td>
                                        <td>₹<%= product.price.toLocaleString('en-IN') %></td>
                                        <td><%= product.stock %></td>
                                        <td><%= product.category %></td>
                                        <td><span class="product-status <%= product.status %>"><%= product.status %></span></td>
                                    </tr>
                                    <% }); %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Recent Orders -->
        <div class="row mt-4">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h5><i class="fas fa-receipt me-2"></i>Recent Orders</h5>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>Order ID</th>
                                        <th>Customer</th>
                                        <th>Products</th>
                                        <th>Total</th>
                                        <th>Status</th>
                                        <th>Date</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% sellerData.orders.forEach(order => { %>
                                    <tr>
                                        <td><%= order.id %></td>
                                        <td><%= order.customerName %></td>
                                        <td><%= order.products.join(', ') %></td>
                                        <td>₹<%= order.total.toLocaleString('en-IN') %></td>
                                        <td><span class="order-status <%= order.status %>"><%= order.status %></span></td>
                                        <td><%= order.date %></td>
                                    </tr>
                                    <% }); %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Chatbot Toggle Button -->
    <button class="chatbot-toggle" onclick="toggleChatbot()">
        <i class="fas fa-comments"></i>
    </button>

    <!-- Chatbot Container -->
    <div class="chatbot-container" id="chatbotContainer">
        <div class="chatbot-header">
            <div>
                <i class="fas fa-shopping-cart me-2"></i>
                <strong>Flipkart Seller Assistant</strong>
            </div>
            <button class="btn btn-sm btn-outline-light" onclick="toggleChatbot()">
                <i class="fas fa-times"></i>
            </button>
        </div>
        <div class="chatbot-messages" id="chatMessages">
            <div class="message bot">
                <i class="fas fa-shopping-cart me-2"></i>
                Hello! I'm your Flipkart seller assistant. I can help you manage your products, orders, and business profile on the Flipkart platform. Try asking me to:
                <ul class="mt-2 mb-0">
                    <li>"Update the price of Wireless Bluetooth Headphones to ₹6999"</li>
                    <li>"Change order ORD003 status to shipped"</li>
                    <li>"Add a new product called Gaming Mouse"</li>
                    <li>"Update my business phone number"</li>
                </ul>
            </div>
        </div>
        <div class="loading" id="loading">
            <i class="fas fa-spinner fa-spin"></i> Processing...
        </div>
        <div class="chatbot-input">
            <div class="input-group">
                <input type="text" class="form-control" id="chatInput" placeholder="Type your message...">
                <button class="btn btn-primary" onclick="sendMessage()">
                    <i class="fas fa-paper-plane"></i>
                </button>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function toggleChatbot() {
            const container = document.getElementById('chatbotContainer');
            const toggle = document.querySelector('.chatbot-toggle');
            
            if (container.style.display === 'none' || container.style.display === '') {
                container.style.display = 'flex';
                toggle.style.display = 'none';
            } else {
                container.style.display = 'none';
                toggle.style.display = 'block';
            }
        }

        async function sendMessage() {
            const input = document.getElementById('chatInput');
            const messages = document.getElementById('chatMessages');
            const loading = document.getElementById('loading');
            const message = input.value.trim();
            
            if (!message) return;
            
            // Add user message
            const userDiv = document.createElement('div');
            userDiv.className = 'message user';
            userDiv.innerHTML = `<i class="fas fa-user me-2"></i>${message}`;
            messages.appendChild(userDiv);
            
            input.value = '';
            loading.style.display = 'block';
            messages.scrollTop = messages.scrollHeight;
            
            try {
                const response = await fetch('/api/seller-chatbot', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ message })
                });
                
                const data = await response.json();
                loading.style.display = 'none';
                
                // Add bot response
                const botDiv = document.createElement('div');
                botDiv.className = 'message bot';
                let botMessage = `<i class="fas fa-robot me-2"></i>${data.response}`;
                
                if (data.actionExecuted && data.updateResult) {
                    botMessage += `<br><br><div class="alert alert-success p-2 mt-2 mb-0">
                        <i class="fas fa-check-circle me-1"></i> ${data.updateResult.message}
                    </div>`;
                }
                
                botDiv.innerHTML = botMessage;
                messages.appendChild(botDiv);
                
                // Refresh page if data was updated
                if (data.actionExecuted) {
                    setTimeout(() => {
                        location.reload();
                    }, 2000);
                }
                
            } catch (error) {
                loading.style.display = 'none';
                const errorDiv = document.createElement('div');
                errorDiv.className = 'message bot';
                errorDiv.innerHTML = `<i class="fas fa-exclamation-triangle me-2"></i>Sorry, I encountered an error. Please try again.`;
                messages.appendChild(errorDiv);
            }
            
            messages.scrollTop = messages.scrollHeight;
        }
        
        // Allow Enter key to send message
        document.getElementById('chatInput').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                sendMessage();
            }
        });
    </script>
</body>
</html> 